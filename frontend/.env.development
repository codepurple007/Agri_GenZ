# Mock API uses placeholder “Gemini” text. For real Gemini + SMS preview from the backend:
#   1. Put GEMINI_API_KEY in backend/.env
#   2. Uncomment the next line and restart `npm run dev`
# VITE_USE_REAL_API=true
# Optional: VITE_API_ORIGIN=http://localhost:3001
